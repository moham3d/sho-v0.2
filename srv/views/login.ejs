<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - Al-Shorouk Radiology Management System</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <link href="/css/custom.css" rel="stylesheet">
</head>
<body>
    <main class="login-container" id="main-content">
        <div class="login-card">
            <header class="login-header">
                <i class="fas fa-hospital-alt fa-3x mb-3" aria-hidden="true"></i>
                <h1 class="mb-2">Al-Shorouk Radiology</h1>
                <p class="mb-0 opacity-75">Healthcare Management System</p>
            </header>
            <div class="login-body">
                <% if (error) { %>
                    <div class="alert alert-danger" role="alert" aria-live="assertive" aria-atomic="true">
                        <i class="fas fa-exclamation-triangle me-2" aria-hidden="true"></i>
                        <%= error %>
                    </div>
                <% } %>
                
                <form action="/login" method="POST" id="loginForm" aria-label="Login form">
                    <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                    <div class="mb-3">
                        <label for="username" class="form-label">
                            <i class="fas fa-user me-2" aria-hidden="true"></i>Username
                        </label>
                        <div class="input-group">
                            <span class="input-group-text"><i class="fas fa-user" aria-hidden="true"></i></span>
                            <input type="text" class="form-control" id="username" name="username" required 
                                   placeholder="Enter your username" value="<%= typeof username !== 'undefined' ? username : '' %>"
                                   aria-describedby="usernameHelp usernameError"
                                   aria-invalid="false">
                        </div>
                        <div class="form-text" id="usernameHelp">Enter your registered username</div>
                        <div id="usernameError" class="invalid-feedback" style="display: none;" role="alert" aria-live="polite">
                            <!-- Username validation error messages will appear here -->
                        </div>
                    </div>
                    
                    <div class="mb-4">
                        <label for="password" class="form-label">
                            <i class="fas fa-lock me-2" aria-hidden="true"></i>Password
                        </label>
                        <div class="input-group">
                            <span class="input-group-text"><i class="fas fa-lock" aria-hidden="true"></i></span>
                            <input type="password" class="form-control" id="password" name="password" required 
                                   placeholder="Enter your password"
                                   aria-describedby="passwordHelp passwordError"
                                   aria-invalid="false">
                        </div>
                        <div class="form-text" id="passwordHelp">Enter your password</div>
                        <div id="passwordError" class="invalid-feedback" style="display: none;" role="alert" aria-live="polite">
                            <!-- Password validation error messages will appear here -->
                        </div>
                    </div>
                    
                    <div class="d-grid">
                        <button type="submit" class="btn btn-primary btn-login" id="loginBtn"
                                aria-describedby="loginStatus">
                            <i class="fas fa-sign-in-alt me-2" aria-hidden="true"></i>
                            <span id="loginBtnText">Sign In</span>
                        </button>
                        <div id="loginStatus" class="sr-only" aria-live="polite">
                            Ready to sign in
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </main>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>