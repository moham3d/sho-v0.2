<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= typeof title !=='undefined' ? title : 'Al-Shorouk Radiology Management System' %>
    </title>
    <!-- Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">

    <!-- Styles -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <link href="/css/custom.css" rel="stylesheet">
    <link href="/css/theme.css" rel="stylesheet">
    <link href="/css/accessibility.css" rel="stylesheet">

    <% if (typeof extraHead !=='undefined' ) { %>
        <%- extraHead %>
            <% } %>
</head>

<body class="bg-light">
    <%- include('../navigation', { currentPage: typeof currentPage !=='undefined' ? currentPage : '' }) %>
        <main class="<%= typeof containerClass !== 'undefined' ? containerClass : 'container' %> py-4" id="main-content"
            role="main">